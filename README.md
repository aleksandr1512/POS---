# POS---Поисковая обучающая система

Запуск(Linux):
```Bash
cd flask-server
python3 -m venv .venv
source .venv/bin/activate
pip install flask
python3 server.py
```

Запуск(Windows):
```
cd flask-server
python3 -m venv .venv
.venv\Scripts\activate
pip install flask
python3 server.py
```
